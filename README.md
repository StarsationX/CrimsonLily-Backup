# CrimsonLily-Backup

Arcane Odyssey script (executor). Run `build/build.lua` in your executor, e.g.

```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/StarsationX/CrimsonLily-Backup/main/build/build.lua"))()
```

Edit files in `src/`, then rebuild with `python build/bundle.py`.
